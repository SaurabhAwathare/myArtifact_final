# Walkthrough – Verify Firebase Project Identity

## Phase 1: Inspect Repository `google-services.json`

I have inspected the `google-services.json` files found in the repository. Both the `debug` and `release` variants point to the same Firebase project.

### Project Information
- **project_id**: `myartifact-555e3`
- **project_number**: `1002864539573`
- **storage_bucket**: `myartifact-555e3.firebasestorage.app`

### Mobile SDK App IDs
- **Debug Variant** (`com.saurabh.artifact.debug`): `1:1002864539573:android:b496a3b85b74d18b290bfc`
- **Release Variant** (`com.saurabh.artifact`): `1:1002864539573:android:b517e49cf083dd97290bfc`

---

## Phase 2 & 3: Runtime Firebase Project Identity

Based on the generated resources for the current build (`debug` variant), the following values are used by `FirebaseApp` at runtime:

| Field | Runtime Value (from `values.xml`) |
| :--- | :--- |
| **Project ID** | `myartifact-555e3` |
| **Application ID** | `1:1002864539573:android:b496a3b85b74d18b290bfc` |
| **GCM Sender ID** | `1002864539573` |
| **Storage Bucket** | `myartifact-555e3.firebasestorage.app` |

> [!NOTE]
> These values were extracted directly from the generated [values.xml](file:///F:/Android Project/01/app/build/generated/res/processDebugGoogleServices/values/values.xml), which the Google Services plugin creates from the `google-services.json` file.

---

## Phase 4: Multiple `google-services.json` Search

I performed a full repository search and found exactly two `google-services.json` files:
1. [app/src/debug/google-services.json](file:///F:/Android Project/01/app/src/debug/google-services.json)
2. [app/src/release/google-services.json](file:///F:/Android Project/01/app/src/release/google-services.json)

Both files are consistent regarding the target Firebase Project ID (`myartifact-555e3`).

---

## Phase 5: Packaged `google-services.json`

The current build variant is **Debug**.
The build system has selected and packaged the configuration from:
`app/src/debug/google-services.json`

This is verified by the existence and content of the generated resource file:
`app/build/generated/res/processDebugGoogleServices/values/values.xml`

---

## Final Assessment

1. **Repository Firebase Project**: `myartifact-555e3`
2. **Runtime Firebase Project**: `myartifact-555e3`
3. **Packaged google-services.json**: `app/src/debug/google-services.json`
4. **Project IDs Match**: **YES**

> [!IMPORTANT]
> The Android application is communicating with the expected Firebase project: **`myartifact-555e3`**.
> There is no mismatch between the repository configuration and the runtime environment.

### Potential Observations from Logs
I noticed in the `firestore-debug.log` that the Firebase Emulator reported requests for several different project IDs (e.g., `myartifact-admin-unique`, `myartifact-phase3`). These were identified as being generated by the **Firestore test suites** (e.g., `investigation_engagement.test.js`) which use `@firebase/rules-unit-testing`. This is standard behavior for the test environment and does not affect the main Android application's production/debug identity.
